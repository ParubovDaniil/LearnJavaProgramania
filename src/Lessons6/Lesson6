Урок 6. Перегрузка методов и конструкторов (overloading). Понятие this
Перегруженные(Overloading) методы это методы которые имеют одинаковые имена и разный список параметров.
Ошибки компилятора-обнаруживает компилятор(синтаксические,симантические ошибки) и RunTime ошибки.
THIS this должен всегда стоять 1ым полем внутри конструктора.

Метод оверлоадинг=одинаковые имена,параметры разные
Конструкторы оверлоадинс=параметр лист разные имена одинаковые
This может обратиться,вызвать оверлоадинг метод